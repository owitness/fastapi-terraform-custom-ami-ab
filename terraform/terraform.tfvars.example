# Copy this file to terraform.tfvars and fill in your values

project_name     = "fastapi"
environment      = "dev"
instance_type    = "t3.micro"
desired_capacity = 2
min_size         = 2
max_size         = 4

# Optional: EC2 key pair for SSH access (leave empty if not needed)
# key_name         = "your-key-name"